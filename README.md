# Hangman-Games
A simple text-based Hangman Game built with Python where players guess a randomly selected word one letter at a time. The project demonstrates the use of random module, while loops, if-else statements, strings, and lists.
